---
# cPanel Git Version Control deployment.
# Copies everything inside public/ (including .htaccess) into this account's web root.
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/
    - /bin/cp -R public/. $DEPLOYPATH
