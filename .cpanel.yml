---
# cPanel Git Version Control deployment.
# Copies everything inside public/ (including .htaccess) into the web root.
# EDIT the DEPLOYPATH line: replace CPANELUSER with the cPanel account's username.
deployment:
  tasks:
    - export DEPLOYPATH=/home/CPANELUSER/public_html/
    - /bin/cp -R public/. $DEPLOYPATH
